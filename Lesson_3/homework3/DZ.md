**Множественные возвращаемые значения**
# Задание:
Создать функцию minMax(arr []int) (int, int), которая возвращает минимальный и максимальный элементы среза. 
Пример:

    min, max := minMax([]int{3, 7, 2, 9})
    fmt.Println(min, max) // 2 9