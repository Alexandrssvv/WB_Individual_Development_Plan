**Передача по значению и по указателю**
# Задание:
Создать две функции:
   * swap(a, b int) — пытается поменять местами значения (по значению, результата не будет).
   * swapPtr(a, b *int) — меняет значения местами через указатели.
Проверить работу в main().