**Встроенные функции**
# Задание:
Сделать функцию sliceOps(), которая:
Создаёт пустой срез []int через make.
Добавляет в него числа от 1 до 5 с помощью append.
Копирует в новый срез.
Возвращает длину среза и его содержимое.